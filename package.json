{
  "name": "youtube-notion-project",
  "version": "1.0.0",
  "description": "Merged Next.js Frontend and FastAPI Backend",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && source .venv/bin/activate && fastapi dev main.py --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
